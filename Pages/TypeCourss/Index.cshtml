@page
@model Universite.Pages.TypeCourss.IndexModel

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeCours[0].Intitule)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TypeCours) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Intitule)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TypeCoursID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TypeCoursID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TypeCoursID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
